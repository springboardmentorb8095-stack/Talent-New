# Run this command to create the migration:
# python manage.py makemigrations projects --name add_estimated_duration

# Then run:
# python manage.py migrate

# This will add the estimated_duration field to the Project model